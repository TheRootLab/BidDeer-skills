# proposal-point-checker

标书逐项检查 Skill。

根据人工提供的审核点清单，对投标文件进行逐项证据定位、要求对齐、出处追溯和核对提示。

## Folders

* `templates/`: 包含 `io-contract.md`，定义输入输出协议。
* `examples/`: 包含示例清单、标书片段和检查结果。
* `keywords/`: 关键字列表。
* `schemas/`: 数据结构定义。
